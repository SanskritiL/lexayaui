{
  "name": "lexaya",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "dev": "npx serve .",
    "vercel:dev": "npx vercel dev --listen 3000",
    "test": "jest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
